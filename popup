#!/bin/sh

. ~/.barconf

# time to stay up
duration=3

args="-d -f $glyphs -f $font -F #$fg -B #$bg -u 3 -g ${length:-250}x${height:-30}+${xpos:-1066}+${ypos:-20}"

# Create the popup and make it live for 3 seconds
(echo "%{c}$@"; sleep ${duration}) | \
    lemonbar ${args} | sh &
